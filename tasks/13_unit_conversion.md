# Task: 13 Unit Conversion
Add a unit conversion feature to the calculator.
- Support common conversions: Length (m to ft), Weight (kg to lb), and Temperature (C to F).
- Syntax should be something like `convert(value, from_unit, to_unit)`.
- Ensure robust error handling for unsupported units.
