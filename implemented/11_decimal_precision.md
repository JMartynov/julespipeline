# Task: 11 Decimal Precision Support
Extend the calculator to support the `decimal` module for high-precision arithmetic.
- Add a configuration option to switch between `float` and `Decimal`.
- Ensure all basic and advanced math operations work with `Decimal` when enabled.
- Add tests verifying precision beyond standard float limitations.
