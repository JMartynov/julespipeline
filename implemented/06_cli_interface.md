# Task: 06 Cli Interface

Create a command-line interface (CLI) using the `argparse` library that allows users to perform calculations directly from the terminal. Write tests to verify the CLI argument parsing and execution.
