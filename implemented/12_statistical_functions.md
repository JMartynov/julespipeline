# Task: 12 Statistical Functions
Implement a set of statistical functions in a new module `src/stats.py`.
- Include `mean`, `median`, `mode`, `variance`, and `standard_deviation`.
- Support passing a list of numbers or a string expression representing a list.
- Integrate these functions into the main `evaluate_expression` parser.
